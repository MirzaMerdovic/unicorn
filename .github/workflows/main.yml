name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Publish to Docker Registry
      uses: mirzamerdovic/github-action-docker-semver@master
      with:
        name: unicorn
        username: mirzamerdovic
        password: MiMer1984
        semver_tag: 1.0.1-p1
        registry: https://index.docker.io/v1/
